<!DOCTYPE html>
<html>
<head>
    <title>Welcome</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Bug Tracker</h1>
    </header>
    <div class="nav-links">
        <a href="/">Back</a>
        <a href="/project/id/creatissue?projectName=<%= encodeURIComponent(project.name) %>">Create Issue</a>
    </div>
   
    <form id="searchForm" action="/user/search/<%=project.name%>" method="post">  
        <label for="label">Filter by label:</label>
        <input type="text" id="label" name="label">
        <label for="author">Filter by Author:</label>
        <input type="text" id="author" name="author">
        <label for="search">Search by Title or Description:</label>
        <input type="text" id="search" name="search">
        <button type="submit" id="searchBtn">Search</button>
    </form>

    <div class="result-section">
        <% if( result.length >= 1) { %>
            <h2>Results</h2>

            <% for (let i = 0; i < result.length; i++) { %>
                <div class="issue-item">
                    <h3>Title: <%= result[i].title %></h3>
                    <p>Description: <%= result[i].description %></p>
                    <p>Labels: <%= result[i].labels %></p>
                    <p>Author: <%= result[i].author %></p>
                </div>
            <% } %>
            <%result=[]%>
        <% } %>
    </div>
 
    <div class="project-details">
        <h2>Project Details</h2>
        <p>Name: <%= project.name %></p>
        <p>Description: <%= project.description %></p>
        <p>Author: <%= project.author %></p>

        <h2>Issue List</h2>
        <% for (let i = 0; i < project.bug.length; i++) { %>
            <div class="issue-item">
                <h3>Title: <%= project.bug[i].title %></h3>
                <p>Description: <%= project.bug[i].description %></p>
                <p>Labels: <%= project.bug[i].labels %></p>
                <p>Author: <%= project.bug[i].author %></p>
            </div>
        <% } %>
    </div>

    <footer>
        <p>&copy; 2023 Your Company. All rights reserved.</p>
    </footer>
</body>
</html>
